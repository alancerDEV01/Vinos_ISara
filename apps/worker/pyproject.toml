[project]
name = "sara-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["dramatiq[redis]>=1.18,<2", "structlog>=25,<26"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sara_worker"]
